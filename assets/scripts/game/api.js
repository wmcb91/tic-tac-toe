'use strict'



const signUp = (data) => {
  // console.log(data);
  return $.ajax({
    url: app.host + '/sign-up',
    method: 'POST',
    headers: {
      Authorization: 'Token token=' + app.user.token,
    },
    data: data,
  });
};

const signUp = (data) => {
  // console.log(data);
  return $.ajax({
    url: app.host + '/sign-up',
    method: 'POST',
    headers: {
      Authorization: 'Token token=' + app.user.token,
    },
    data: data,
  });
};

const signUp = (data) => {
  // console.log(data);
  return $.ajax({
    url: app.host + '/sign-up',
    method: 'POST',
    headers: {
      Authorization: 'Token token=' + app.user.token,
    },
    data: data,
  });
};

const signUp = (data) => {
  // console.log(data);
  return $.ajax({
    url: app.host + '/sign-up',
    method: 'POST',
    headers: {
      Authorization: 'Token token=' + app.user.token,
    },
    data: data,
  });
};

const signUp = (data) => {
  // console.log(data);
  return $.ajax({
    url: app.host + '/sign-up',
    method: 'POST',
    headers: {
      Authorization: 'Token token=' + app.user.token,
    },
    data: data,
  });
};
